from typing import Callable


def execute_operation(
    a: float | int,
    b: float | int,
    func: Callable[[float | int, float | int], float | int],
) -> float | int:
    """
    Принимает два числа и функцию, реализующую операцию.
    Это 'чистая' функция высшего порядка.
    """
    return func(a, b)


operations: dict[str, Callable[[float | int, float | int], float | int]] = {
    "+": lambda x, y: x + y,
    "-": lambda x, y: x - y,
    "*": lambda x, y: x * y,
    "/": lambda x, y: x / y,
    "^": lambda x, y: x**y,
    "%": lambda x, y: x % y,
}


def main():
    x, y = 11, 2

    print(f"Исходные числа: {x} и {y}\n" + "-" * 20)

    for symbol, func in operations.items():
        result = execute_operation(x, y, func)
        print(f"Операция [{symbol}]: {result}")


if __name__ == "main":
    main()


# Задание 1: "Броня" от деления на ноль (try/except)
# Проблема: Если в x, y = 11, 0, программа упадет на операциях / и %.
# Задача: Измени функцию execute_operation так, чтобы при возникновении ошибки деления на ноль она не прерывала программу,
# а возвращала строку "Ошибка: деление на ноль".

# Цель: Отработать базовый перехват конкретного системного исключения.

# Задание 2: Безопасный ввод и цикл (ValueError)
# Проблема: В коде жестко прописаны x, y = 11, 2.
# Задача: 1. Реализуй ввод чисел через input().
# 2. Оберни ввод в блок try/except, чтобы перехватывать ValueError (если пользователь ввел буквы).
# 3. Используй цикл while, чтобы программа спрашивала число до тех пор, пока не будет введено корректное значение.

# Цель: Научиться обрабатывать ошибки пользовательского ввода.

# Задание 3: Проверка типов "на входе" (assert)
# Проблема: В Python можно попытаться сложить строку и число, что вызовет TypeError.
# Задача: В начале функции execute_operation добавь инструкцию assert, которая проверяет,
#  что оба аргумента a и b являются числами (int или float). Если это не так, assert должен выдать сообщение:
# "Аргументы должны быть числовыми!".

# Цель: Понять разницу между обработкой исключений и принудительной проверкой условий.

# Задание 4: Кастомное исключение и raise
# Проблема: Операция возведения в степень (^) может "повесить" компьютер, если передать слишком большие числа.
# Задача: 1. Создай свой класс исключения TooPowerfulError.
# 2. В блоке main или внутри execute_operation добавь проверку: если выполняется операция ^ и число y > 1000,
# инициируй свою ошибку через raise.
# 3. Обработай это исключение в цикле, чтобы программа просто пропускала эту операцию и шла дальше.

# Цель: Научиться создавать и вызывать собственные исключения.

# Задание 5: Отказоустойчивый калькулятор (else / finally)
# Задача: Доработай цикл в main так, чтобы:

# В блоке else выводилось сообщение: "Операция выполнена успешно".

# В блоке finally выводилось сообщение: "Завершение вычисления для [символ_операции]".

# Попробуй намеренно вызвать ошибку (например, KeyError, обратившись к несуществующей операции) и посмотри, как сработает finally.

# Цель: Закрепить понимание полной структуры блока обработки ошибок и логики работы else/finally.
